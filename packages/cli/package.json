{
  "name": "@openspec-diff/cli",
  "version": "1.0.0",
  "private": true,
  "description": "Go-based CLI for openspec-diff.",
  "scripts": {
    "build": "go build -o ./bin/openspec-diff ./cmd/openspec-diff",
    "format": "test -z \"$(gofmt -l .)\"",
    "test": "go test ./..."
  },
  "license": "GPL-3.0-only"
}
